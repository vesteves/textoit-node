## Starting

1- Run `yarn` or `npm install`;

2- Run `yarn dev` or `npm run dev`;

3- Wait the server until the message `The file was processed. The server is ON!` appears;

4- Hit GET `/film` to get the min and max as expected;