## Iniciando o projeto

1- Run `yarn` or `npm install`;

2- Wait the server until the message `The file was processed. The server is ON!` appears;

3- Hit GET `/film` to get the min and max as expected;